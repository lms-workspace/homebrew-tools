# LMS Homebrew tools

The former `tally` formula was retired because it installed an obsolete Python implementation that conflicted with the canonical Go CLI.

Tally is currently in private dogfood. Build the Go implementation from [blakep-lms/Tally](https://github.com/blakep-lms/Tally) until an approved Go release publishes a replacement package.
